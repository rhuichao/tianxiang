<%= form_tag(withdraws_path) %>
	<div class="field">
    <%= label_tag("year", "年份: ") %>
    <%= text_field_tag('year', @year) %>
    <%= label_tag("year", "月份: ") %>
    <%= text_field_tag('month', @month) %>
  </div>
	<table class="table table-striped">
	  <tr>
	    <th>工号</th>
	    <th>姓名</th>
			<th>支取金额</th>
	  </tr>
	 
		<% @employees.each do |employee| %>
	  <tr>
			
	    <td><%= employee.id_card %></td>
	    <td><%= employee.name %></td>
			<td>
				<%= hidden_field_tag('withdraws[][employee_id]', employee.id) %>
				<%= text_field_tag('withdraws[][amount]') %>
			</td>
	  </tr>
		<% end %>
	</table>
  <div class="actions">
    <%= submit_tag "保 存" %>
  </div>
<% $end %>
<br/>